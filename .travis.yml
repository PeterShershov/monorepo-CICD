sudo: false
language: node_js

node_js:
  - 8
  - 10

cache: yarn

jobs:
  include:
    - stage: "Linting"
      script: yarn lint
    - stage: "Testing"
      script: yarn test
    - stage: "Publishing"
      script: yarn release
